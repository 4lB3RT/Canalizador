name: helmreel

x-php-image: &php-image europe-west1-docker.pkg.dev/galvanic-camp-493316-i2/helmreel-images/php-base:php-8.5-v1
x-mysql-image: &mysql-image europe-west1-docker.pkg.dev/galvanic-camp-493316-i2/helmreel-images/mysql:mysql-8.0-v1

services:
  nginx:
    image: nginx:1.27-alpine
    container_name: nginx_helmreel_prod
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ${NGINX_CONF_FILE:-./nginx.conf}:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /opt/helmreel/web:/var/www/helmreel:ro
      - /opt/helmreel/app:/code:ro
    depends_on:
      - php
    networks:
      helmreel:

  php:
    image: *php-image
    container_name: php_helmreel_prod
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - /opt/helmreel/app:/code
      - vendor:/code/vendor
      - /opt/helmreel/data/storage:/code/storage
      - /tmp/helmreel:/tmp/helmreel
      - ./opcache.ini:/usr/local/etc/php/conf.d/opcache.ini:ro
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
      rabbitmq:
        condition: service_healthy
    networks:
      helmreel:

  worker:
    image: *php-image
    container_name: worker_helmreel_prod
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - /opt/helmreel/app:/code
      - vendor:/code/vendor
      - /opt/helmreel/data/storage:/code/storage
      - /tmp/helmreel:/tmp/helmreel
    depends_on:
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    working_dir: /code
    command: >
      sh -c "php artisan queue:work rabbitmq
        --queue=youtube.video.created,youtube.short.created,video.created,clip.created,clip.generated,clip.completed,video.all_clips_completed
        --tries=3 --timeout=300 --sleep=3"
    networks:
      helmreel:

  mysql:
    image: *mysql-image
    container_name: mysql_helmreel_prod
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - /opt/helmreel/data/mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root --password=$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      helmreel:

  redis:
    image: redis:7-alpine
    container_name: redis_helmreel_prod
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - /opt/helmreel/data/redis:/data
    networks:
      helmreel:

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: rabbitmq_helmreel_prod
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
    volumes:
      - /opt/helmreel/data/rabbitmq:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      helmreel:

volumes:
  vendor:

networks:
  helmreel:
    driver: bridge
