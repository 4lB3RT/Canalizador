server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    client_max_body_size 200M;
    error_log  /var/log/nginx/canalizador.error.log;
    access_log /var/log/nginx/canalizador.access.log;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml application/json application/javascript application/xml+rss application/atom+xml image/svg+xml;

    root /var/www/canalizador;
    index index.html;

    location /api {
        rewrite ^/api/?(.*)$ /$1 break;
        try_files $uri $uri/ /index.php?$query_string;
        root /code/public;
    }

    location = /up {
        try_files $uri /index.php?$query_string;
        root /code/public;
    }

    # Only OAuth callbacks go to the backend; /login, /register,
    # /forgot-password and /reset-password are client-side SPA routes
    # (handled by location / -> index.html). API auth runs over /api.
    location ~ ^/auth(/.*)?$ {
        try_files $uri $uri/ /index.php?$query_string;
        root /code/public;
    }

    location ~ \.php$ {
        try_files $uri =404;
        root /code/public;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_param APPLICATION_ENV production;
        fastcgi_read_timeout 600s;
    }

    location / {
        try_files $uri $uri/ /index.html =404;
    }
}
